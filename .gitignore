# Python
__pycache__/
*.py[cod]
.venv/
dist/
build/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Trained/downloaded model files — keep large binaries out of git.
# Un-ignore specific committed models deliberately if needed.
models/
*.onnx
*.tflite

# faster-whisper / huggingface download caches, if redirected locally
.cache/

# Training: datasets/outputs are gigabytes, configs are generated (make_configs.py)
training/data/
training/output/
training/config/

# OS noise
Thumbs.db
.DS_Store
