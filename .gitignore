# Python
__pycache__/
*.py[cod]
.venv/
dist/
build/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Deployed models in models/ are versioned deliberately (~215 KB each);
# training intermediates stay out via training/output/ below.
*.tflite

# faster-whisper / huggingface download caches, if redirected locally
.cache/

# Training: datasets/outputs are gigabytes, configs are generated (make_configs.py)
training/data/
training/output/
training/config/

# OS noise
Thumbs.db
.DS_Store
